# Генерування нового ssh-ключа

ssh-keygen

# Залишаємо поля за замовчуванням
# Вводимо пароль для захисту ssh-ключа

# Ключі будуть згенеровані по шляху /~/.ssh/
# Завантажуємо ключ <id_rsa.pub> на сайт GitHub
